#suma y resta (+ y -)
suma = 12 + 5
resta = 12 -5 

#multi y división (* y /)
multi = 12 * 5
division = 12 / 5 #devuelve un dato float (flotante)

#potenciación (exponente) (**)
exponente = 12 ** 5

#division baja (//)
division_baja = 12 // 5 #devuelve entero redondeado hacia abajo

#resto o modulo
resto = 12 % 5

#type(dato) nos devuelve que tipo de dato es
tipo_de_dato = type(division_baja)

print(tipo_de_dato)

# No existen numeros enteros que tengan coma
# 2 = numero int (entero)
# 2.0 = numero float (flotante)
# La función type() es para ver el tipo de dato
# La división baja devuelve un entero.
# En la división baja, los resultados siempre los convierte en un entero y los redondea hacia abajo, por más que sean numeros con coma (.). De ahí viene el termino "division baja".
# El operador resto muestra lo que sobra. Se suele usar cuando se trabaja con datos.